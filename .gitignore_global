# Global .gitignore file
# Add to repo with
# $ git config --global core.excludesfile ~/.gitignore_global

# general
fmca_*
*venv/
.vscode/
.netrc

# build
build/
*.egg-info/

# docker-aosp-build
retries-host.log/
